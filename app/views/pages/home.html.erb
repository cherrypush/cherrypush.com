<% title 'Home' %>

<div class="mr-auto place-self-center lg:col-span-7 text-center mt-24 mb-36">
  <h1 class="max-w-3xl mb-4 text-4xl font-extrabold tracking-tight md:text-5xl xl:text-6xl dark:text-white mx-auto">
    Track and manage your <span class="text-blue-700 dark:text-blue-600">technical debt</span> with ease.
  </h1>
  <p class="max-w-2xl mb-6 font-light text-gray-500 lg:mb-8 md:text-lg lg:text-xl dark:text-gray-400 mx-auto">
    Cherry helps you track your technical debt, capture the value of your work, and visualize the impact of your team.
  </p>
  <div class="flex flex-col md:flex-row justify-center">
    <a
      href="<%= github_sign_in_path %>"
      class="btn-primary text-lg py-3 px-7 mb-3 md:mb-0 md:mr-3 uppercase flex items-center justify-center"
    >
      <span class="mr-2">Try Cherry for free</span>
      <%= heroicon 'arrow-right', options: { class: 'inline' } %>
    </a>
    <%= render 'components/watch_the_video_button' %>
  </div>
</div>

<section class="text-center rounded-lg bg-slate-100 dark:bg-gray-900 mb-12 p-6 md:p-12">
  <div class="text-gray-500 dark:text-gray-400 mb-9">
    Since our first beta users arrived, <%= time_ago_in_words Date.new(2022, 12, 29) %> ago 🎉
  </div>
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
    <div class="flex card flex-col items-center justify-center">
      <dt class="mb-2 text-3xl md:text-4xl font-extrabold">
        <%= number_to_human(Report.all.map(&:total).sum) %>
      </dt>
      <dd class="font-light text-gray-500 dark:text-gray-400">tracked issues</dd>
    </div>
    <div class="flex card flex-col items-center justify-center">
      <dt class="mb-2 text-3xl md:text-4xl font-extrabold">
        <%= number_to_human Report.count %>
      </dt>
      <dd class="font-light text-gray-500 dark:text-gray-400">submitted reports</dd>
    </div>
    <div class="flex card flex-col items-center justify-center">
      <dt class="mb-2 text-3xl md:text-4xl font-extrabold"><%= number_to_human User.count %></dt>
      <dd class="font-light text-gray-500 dark:text-gray-400">active users</dd>
    </div>
    <div class="flex card flex-col items-center justify-center">
      <dt class="mb-2 text-3xl md:text-4xl font-extrabold">1</dt>
      <dd class="font-light text-gray-500 dark:text-gray-400">place to go 🍒</dd>
    </div>
  </div>
</section>

<section class="rounded-xl bg-slate-100 dark:bg-gray-900 mb-12 p-6 md:p-12">
  <div class="gap-8 items-center mx-auto max-w-screen-xl xl:gap-16 md:grid md:grid-cols-2">
    <%=
      image_tag 'screenshot_metrics.png',
                class: 'rounded-lg w-full hover:scale-105 transition drop-shadow hover:drop-shadow-xl'
    %>
    <div class="mt-4 md:mt-0">
      <h2 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white">
        Stay on top of your code migrations 🏔
      </h2>
      <p class="font-light text-gray-500 md:text-lg dark:text-gray-400">
        Visualize the progress of your migrations. <br />
        Watch out for new technical debt.
      </p>
    </div>
  </div>
</section>

<section class="rounded-xl bg-slate-100 mb-12 p-6 md:p-12">
  <div class="gap-16 items-center mx-auto max-w-screen-xl lg:grid lg:grid-cols-2">
    <div class="font-light text-gray-500 sm:text-lg dark:text-gray-400">
      <h2 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white">
        No sensitive data collected 🤫
      </h2>
      <p class="mb-4">
        Cherry relies on an <a href="http://npmjs.com/package/cherrypush">open-source CLI</a>. <br />We don't even have
        access to your code base.
      </p>
    </div>
    <%= image_tag 'cli.gif', class: 'rounded-lg w-full hover:scale-105 transition drop-shadow hover:drop-shadow-xl' %>
  </div>
</section>

<section class="rounded-xl bg-slate-100 dark:bg-gray-900 mb-12 p-6 md:p-12">
  <div class="gap-8 items-center mx-auto max-w-screen-xl xl:gap-16 md:grid md:grid-cols-2">
    <%=
      image_tag 'screenshot_github_actions.png',
                class: 'rounded-lg w-full hover:scale-105 transition drop-shadow hover:drop-shadow-xl'
    %>
    <div class="mt-4 md:mt-0">
      <h2 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white">
        Integrate with GitHub Actions 🤖
      </h2>
      <p class="font-light text-gray-500 md:text-lg dark:text-gray-400">
        Copy and paste a few lines of code, and your metrics are updated on every commit.
      </p>
    </div>
  </div>
</section>

<section class="rounded-xl bg-slate-100 mb-12 p-6 md:p-12">
  <%= render 'components/pricing' %>
</section>
