<!--
<script type="text/javascript">
if (!window.CRISP_WEBSITE_ID) {
  window.$crisp=[];
  window.CRISP_WEBSITE_ID="0a749ad8-c6b6-4724-8a7c-44e6176d6c9c";
  (function(){
    d=document;s=d.createElement("script");
    s.src="https://client.crisp.chat/l.js";
    s.async=1;
    d.getElementsByTagName("head")[0].appendChild(s);
  })();

  <% if current_user %>
    $crisp.push(["set", "user:email", "<%= current_user.email %>"]);
  <% end %>
}
</script>
-->

<!-- Brevo Conversations -->
<script>
    <% if current_user %>
    window.BrevoConversationsSetup = { visitorId: '<%= current_user.signed_id %>' };
    <% end %>

    (function(d, w, c) {
        w.BrevoConversationsID = '64970c055f781c0b9e3f8b99';
        w[c] = w[c] || function() {
            (w[c].q = w[c].q || []).push(arguments);
        };
        var s = d.createElement('script');
        s.async = true;
        s.src = 'https://conversations-widget.brevo.com/brevo-conversations.js';
        if (d.head) d.head.appendChild(s);
    })(document, window, 'BrevoConversations');

    <% if current_user %>
    BrevoConversations('updateIntegrationData', {
      email: '<%= current_user.email %>',
      firstName: '<%= current_user.name.split.first %>',
      lastName: '<%= current_user.name.split[1..].join(' ') %>',
    })
    <% end %>
</script>
<!-- /Brevo Conversations -->
